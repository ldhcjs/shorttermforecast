import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.shorttermforecast',
  appName: 'shorttermforecast',
  webDir: 'dist'
};

export default config;
